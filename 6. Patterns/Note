**Nested Loop**
Nested loop means in loop when we use another loop in the main loop that is called Nested Loop.

//Structure:
for(int i=0;i<4;i++){ //This is outer loop
    for(int j=1;j<=5;j++){ // This is inner loop
        System.out.println(" Content ")  //Print he content
    }
    System.out.println(" ") //Print the content
}

